name: Codex Auto Complete (Legacy Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Governance V2 owns the backlog
        run: |
          echo "Legacy Codex TODO automation is disabled."
          echo "Governance V2 uses the workspace-root TODO.md through the approved TODO Runner."
          echo "This workflow is retained only as migration history and performs no queue mutation."
