name: Codex Queue (Legacy Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Governance V2 owns the backlog
        run: |
          echo "Legacy repository-local Codex queue sync is disabled."
          echo "The workspace-root TODO.md is the sole active Governance V2 backlog."
          echo "This workflow performs no issue, PR, or TODO mutation."
