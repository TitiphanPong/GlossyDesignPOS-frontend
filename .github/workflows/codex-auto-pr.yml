name: Codex Auto PR (Legacy Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Governance V2 owns the backlog
        run: |
          echo "Legacy Codex auto-PR queue execution is disabled."
          echo "Do not select work from a repository-local TODO.md."
          echo "The workspace-root TODO.md is the sole active Governance V2 backlog."
